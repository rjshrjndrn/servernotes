- Create Multiple stuff at once

```sh
~/Documents/projects/ntp-devops(preprod ✗) mkdir -p ansible_poc/{core,LP,DP}/inventory/{dev,staging}/group_vars
~/Documents/projects/ntp-devops(preprod ✗) tree ansible_poc
ansible_poc
├── core
│   └── inventory
│       ├── dev
│       │   └── group_vars
│       └── staging
│           └── group_vars
├── DP
│   └── inventory
│       ├── dev
│       │   └── group_vars
│       └── staging
│           └── group_vars
└── LP
    └── inventory
        ├── dev
        │   └── group_vars
        └── staging
            └── group_vars

 18 directories, 0 files
```
